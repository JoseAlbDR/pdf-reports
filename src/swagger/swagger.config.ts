import { SwaggerConfig } from './swagger.interface';

export const SWAGGER_CONFIG: SwaggerConfig = {
  title: 'Reports Server Service',
  description: 'Reports Server Service API',
  version: '1.0',
  tags: ['Reports Server Service'],
};
